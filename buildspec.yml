version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install
  build:
    commands:
      - echo Build phase started
      - echo "No specific build step required for Express app"
  post_build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - '**/*'
